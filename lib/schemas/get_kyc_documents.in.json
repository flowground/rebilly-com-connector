{
    "type": "object",
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 0,
            "maximum": 1000
        },
        "offset": {
            "type": "integer",
            "minimum": 0
        },
        "filter": {
            "type": "string"
        },
        "sort": {
            "type": "array",
            "items": {
                "enum": [
                    "id",
                    "-id",
                    "createdTime",
                    "-createdTime",
                    "updatedTime",
                    "-updatedTime"
                ],
                "type": "string"
            }
        },
        "Accept": {
            "type": "string",
            "enum": [
                "application/json"
            ],
            "default": "application/json"
        }
    }
}